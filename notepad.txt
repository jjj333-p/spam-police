/*else if (scannableContent.includes("xanax") || scannableContent.includes("molly") || scannableContent.includes("oxy") || scannableContent.includes("coke") || scannableContent.includes("meth")){

        await client.sendEvent(roomId, "m.reaction", ({
            "m.relates_to": {
                "event_id":event["event_id"],
                "key":"💊",
                "rel_type": "m.annotation"
            }
            
        }))

        await client.sendEvent(roomId, "m.reaction", ({
            "m.relates_to": {
                "event_id":event["event_id"],
                "key":"🙅",
                "rel_type": "m.annotation"
            }
        }))

        await client.sendEvent(roomId, "m.reaction", ({
            "m.relates_to": {
                "event_id":event["event_id"],
                "key":"☠️",
                "rel_type": "m.annotation"
            }
        }))

    } */